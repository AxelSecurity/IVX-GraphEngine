[pytest]
asyncio_mode = auto
addopts = -m "not integration"
markers =
    integration: real network calls to external services (e.g. httpbin.org, example.com). Skipped by default; run explicitly with -m integration.
