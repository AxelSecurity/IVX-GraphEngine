playwright>=1.60.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
# lxml — scelto al posto di beautifulsoup4 perché:
# (1) parsing di DOM reali (spesso malformati/non-XHTML) a velocità C,
# (2) piu leggero in dipendenze, senza il layer BeautifulSoup sopra,
# (3) supporto XPath nativo per query complesse sugli alberi (selector extraction).
lxml>=5.1.0
pytest
pytest-asyncio
# Azure AI Foundry Agents SDK — L5 classification.
# Required only when --classify is used; the engine works without it.
azure-ai-projects>=0.1.0
azure-identity>=1.16.0
# Azure AI Vision SDK — arricchimento L5 (OCR sugli screenshot).
# Opzionale: senza di essa l'OCR degrada a error nel dict del bundle,
# ma la Brand Detection (REST legacy) e la classificazione funzionano.
azure-ai-vision-imageanalysis>=1.0.0
aiohttp>=3.9.0  # trasporto async della SDK azure-ai-vision-imageanalysis (OCR): senza, l'OCR fallisce con "No module named 'aiohttp'" anche se la SDK è installata
pyyaml>=6.0
tldextract>=5.0  # Public Suffix List ufficiale — sostituisce lista TLD manuale incompleta
httpx>=0.28.0  # HTTP client asincrono — L2 OSINT, coerenza con architettura async
aiosqlite>=0.20.0  # async SQLite — persistenza append-only
mmh3>=5.0  # MurmurHash3 — favicon hash (stile Shodan/Censys) per L3
fastapi>=0.115.0  # HTTP API — interfaccia asincrona per dashboard/consumatori
uvicorn[standard]>=0.30.0  # Server ASGI — avvio: uvicorn graph_engine.api.app:app --reload
